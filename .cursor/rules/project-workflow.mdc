---
description: Project workflow rules (plan/execute/docs/report) for GoHome
alwaysApply: true
---

# 项目工作流规则（准确优先）

## 规划阶段（严格少假设）

- 在给出最终计划前，至少进行 **3 轮关键确认**，避免一次性大而全方案。
- 至少确认以下两类中的任意 2 项：
  - **范围/阶段**：最小可用 vs 一步到位；是否分阶段实施
  - **验收标准**：功能正确/可维护性/UI表现/编辑器可配/性能等优先级
- 当需求含糊、存在多解或可能返工时：**先问清楚再出计划**。
- 每轮提问至少 2 个关键问题；用户回答后先复述确认再继续。

## 询问方式（必须用选项表单）

- 只要需要用户确认范围/优先级/实现分支/是否执行，默认使用 **AskQuestion 选项表单**，不要只发纯文本追问。
- 选项要求：
  - 至少 2 个可选项；必要时允许多选
  - 文案简短、可直接决策，避免开放式大段输入
  - 对存在默认值的问题，应提供“推荐项”或“最小方案”选项
- 当用户明确要求“给我表单”时，必须立即给出表单，不要先文字解释一轮再问。
- 仅在用户问题是纯事实查询且不需要其决策时，才可以不使用表单直接回复。

## 文档命中优先（避免跑偏）

- 优先阅读顺序（推荐流程）：
  1. 先读项目根 `README.md`（入口跳转），进入知识库首页
  2. 再读 `GoHome_Docs/README.md` 的目录，定位目标模块/章节
  3. 再进入对应子文档，按“常见修改路径”实施
- 若目录与手册均未覆盖当前需求：**先向用户询问**，再扩展文档或实施改动。

## 执行阶段（默认 new agent）

- 当用户明确要求“实现/落地代码”时，默认在 **new agent** 中执行修改。
- 若用户明确要求仅讨论方案/先规划：遵从用户指令，不直接改代码。

## 实现回报结构（默认）

- 修改文件清单
- 每个文件关键改动
- 验证结果（至少包含 `ReadLints`）
- 需要用户在 Unity Inspector 手动补的字段/绑定项

## 使用手册 vs DEVLOG

- `GoHome_Docs/` 是长期**修改指南/模块介绍**（分层知识库），不是编译日志。
- 编译后的过程记录、当日工作总结统一写入 `DEVLOG.md`。
- 仅当“指南内容本身发生变化”时，才更新使用手册。

## 今日报告

- 当用户明确说“生成今日报告”时，按既定格式输出（中文、可扫描、基于真实改动不编造）。
- 当用户要求“生成今日报告并写入 DEVLOG.md”时：生成 + 写入 + 聊天回显同内容。
- 同一天重复生成：默认追加写入 `DEVLOG.md` 当天段落末尾（不覆盖当天旧内容）。

