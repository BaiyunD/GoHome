# 背包与物品注册（Inventory + ItemRegistry?
朡覆盖“物品何定?注册、何进入背包UI 如何展示/交互”的俔入口与验证点?
> 架构背景：[`../02_核心架构/模块划分.md`](../02_核心架构/模块划分.md)

## 概念与边?
- **运时库?*：由 `InventoryManager` 持有（数?变更事件?- **物品定义/注册**：由 `ItemRegistry`（或同类注册衼承载（把 ID/Prefab/展示信息关联起来?- 事件/奖励叺“发放物?ID 与数量，不直接操作 UI

## 朡盺定（推荐?
- UI 刷新?`UIManager.UpdateInfo()` 统一驱动，背包面板只负责 `RefreshView()`
- 新物品的最矗玼
  1. 注册表能找到该物品（?ID?  2. `InventoryManager` ?Add/Remove 并发出变更事?  3. `InventoryPage` 能显示，并能打开详情/使用

## 入口链路（从哇进）

1. 发放/移除（业务侧?   - 通常在事件结果或战斗结算丧?2. 库存管理（权威源?   - `Assets/Scripts/Manager/InventoryManager.cs`
3. 物品注册（定义与映射?   - `Assets/Scripts/Items/PrefabItems/ItemRegistry.cs`
4. UI 展示与交?   - `Assets/Scripts/UIs/InventoryPage.cs`
   - `Assets/Scripts/UIs/PanelDetailsPanel.cs`

## 如何俔（最小改动路径）

### 1) 新三?
- 先做“注册能找到?  - ?`ItemRegistry` 增加新物品条盼ID、Prefab/图标/名称等）
- 再做“能进背包?  - 用现有的奖励/事件跾调用 `InventoryManager` 的添加接?- 后做“UI 且可交互?  - `InventoryPage` 列表能显示 ID 的条?  - `PanelDetailsPanel` 能打并显示?
### 2) 俔背包 UI 刷新/交互

- 优先叔 UI 文件?  - `InventoryPage.RefreshView()`（列表刷新）
  - `PanelDetailsPanel`（?使用按钮?- 不?UI 里直接改库存（避免数捸显示互相调用”致循玼

## 常错与症?
- 背包里出现空白条盝：注册表找不到该物?ID（显示信恏不到?- 数量变了?UI 不变：漏?`UpdateInfo()` 或事件?解绑不（重?漏回调）
- 点击使用没反应：详情面板按钮朻定或使用逻辑朎?
## 验证清单

- 发放物品后：背包列表出现该物品，数量正确
- 移除物品后：数量减少/归零时条理合?- 打开详情：显示名?描述/图标正常，按钏?- 缺注?缺绑定：能给出可定位的错诗志（指出物品 ID 或缺失字段）

