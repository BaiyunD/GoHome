# 协程与状态机：重复触?并发流的排查

朡聚焦“点击按钐吊协程的流程，常昼**重触发**?*协程并发**?*状没恢**?
## 概念与边?
- 协程朴：把条流程拆成帧执行（?UI 等待/动画/面板交互?- 状机盚：防止流程未结束时再次进入（避免扣资源两次开面板多?
朡盚典型例子：`AdvanceFlowController`（`Idle/Running/Transit`）?
## 朡盺定（推荐?
- **入口处就挡住重触发**：`TryAdvance/TryExplore` 看到?`Idle` 直接拒绝
- **有中斷径必须恢复状?*：任?`yield break`/异常/失败都回到叺互状?- **状切换?UI/流程边界对齐**?  - `Running`：纯逻辑阶（扣消推进抽签）
  - `Transit`：进入事?战斗面板的过渡阶段（等待玩交互或战斗结算）

## 常错与症?
- 次点击扣了两?energy/hunger（重复协程）
- 随机事件面板打开两（并发进入）
- 世后按钆也点不了（状态没?`Idle`?
## 如何俔（排查骤）

1. 在入口打印状与帧信恼临时?   - 昐出现“同帧次调用?2. 找到有可能的出路?   - 正常结束
   - 配置缺失（严格失败）
   - UI 引用缺失（严格失败）
   - 外部打断（例如切场景/关闭面板?3. 硿每条跾都做了同件事
   - 恢状?   - 恢 UI（例?ActionBar 昐应重新叔?
## 验证清单

- 连续忟点击不会产生并发协程（扣一次资源）
- 任意失败都能恢叺互状态（不会卡?- 事件/战斗结束后能继续前进/探索

