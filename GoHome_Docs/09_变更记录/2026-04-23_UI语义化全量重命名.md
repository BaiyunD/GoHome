# 2026-04-23 UI语义化全量重命名

## 范围

- 统一 `PanelType` Page 命名，保持枚举顺序不变。
- 重命名 UI MonoBehaviour 类与脚本文件（Page/Panel/Modal 语义化）。
- 为 `UIManager`、`InGameSettingsController` 的关键 `[SerializeField]` 字段补 `FormerlySerializedAs` 兼容。
- 同步 `02/03/05` 与导航文档。

## PanelType 对照（顺序不变）

- `HUDStatus` -> `HudStatus`
- `ActionButtons` -> `ActionBar`
- `Inventory` -> `InventoryPage`
- `Craft` -> `CraftPage`
- `Traits` -> `TraitsPage`
- `Rest` -> `RestPage`
- `End` -> `EndPage`
- `RandomEvent` / `Battle` / `CombatStats` 保持不变

## 类名/文件对照

- `HUDStatusPanel` -> `HudStatusPanel`
- `ActionButtons` -> `ActionBarPanel`
- `InventoryPanel` -> `InventoryPage`
- `CraftPanel` -> `CraftPage`
- `TraitPanel` -> `TraitsPage`
- `RestSummaryPanel` -> `RestPage`
- `EndPanel` -> `EndPage`
- `EventNarrationPanel` -> `EventNarrationModal`
- `SharedResultToast` -> `ResultToastModal`

## 分层约束

- `EventNarrationModal` / `ResultToastModal` 属于 Modal 轻覆盖层（非 `PanelType`）。
- 打开 Page 时，路由仍会主动执行 narration hide（行为保持不变）。

## 兼容说明

- `UIManager`：
  - `hudStatusPage` -> `hudStatusPanel`
  - `actionButtons` -> `actionBarPanel`
  - `eventNarrationPanel` -> `eventNarrationModal`
  - `inventoryPanel` -> `inventoryPage`
  - `craftPanel` -> `craftPage`
  - `restSummaryPanel` -> `restPage`
  - `endPanel` -> `endPage`
  - `traitPanel` -> `traitsPage`
- `InGameSettingsController`：
  - `actionButtons` -> `actionBarPanel`
