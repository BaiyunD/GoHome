# 战斗与事件常见异常（联动排查?
朡集中处理“事??遇敌 ?战斗 ?UI”这条链跸常的异常：不了战敌人信恸 0、面板开关不对刷新顺序错?
## 概念与边?
- 事件侧负责：筛事件执行结果遇敌只返回 `enemyId`
- 战斗侧负责：?`enemyId` + `regionCode` 解析敌人并初始化战斗
- UI 侧负责：?UIManager 策略打开面板、调?`UpdateInfo()` 刷新

## 常异常与定?
### 1) 遇敌事件触发了，但没有进入战?
优先查：
- 事件结果类型昐真的昁敌（而不昖?奖励?- 遇敌结果里的 `enemyId` 昐为空
- 流程控制器是否在拿到 `enemyId` 后调用了 `StartBattleByEnemyId`

参：
- [`../03_功能模块/随机事件与遇敌中?md`](../03_功能模块/随机事件与遇敌中?md)

### 2) 进入战斗了，但敌人数捸 0/?
优先查：
- `EnemyPoolService` 昐存在且配罺敌人列表
- 当前 `regionCode` 的敌人池昐为空
- `enemyId` 在地区池内昐存在

参：
- [`../03_功能模块/战斗与敌人数?md`](../03_功能模块/战斗与敌人数?md)

### 3) 战斗面板打开了，但动按钲关（或关了太多）

推荐策略?- 打开 Battle 面板?**叅?`ActionBar`**
- 不?Battle/RandomEvent 设为狍（否则会隐藏常驻信息栏）

参：
- [`../05_UI与交?面板管理与独占策?md`](../05_UI与交?面板管理与独占策?md)

### 4) HP 文本不刷?报空引用

优先查：
- `BattlePanel` ?`enemyHpText` 昐?Inspector 绑定
- `RefreshView()` 昐对未绑定字做了 `Debug.LogError`
- 昐在开战后调用?`UIManager.UpdateInfo()`

参：
- [`../05_UI与交?Inspector绑定与严格校?md`](../05_UI与交?Inspector绑定与严格校?md)
- [`../05_UI与交?信息刷新链路UpdateInfo.md`](../05_UI与交?信息刷新链路UpdateInfo.md)

## 验证清单

- 遇敌事件叨定进入战斗（非偶现）
- 敌人 HP 首显示不为 0（除非敌人模板就?0?- 面板关合则：叅 ActionBar，常驻信恠保持

